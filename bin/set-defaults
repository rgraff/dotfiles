#!/bin/sh
#
# Sets OS X defaults by running osx/set-defaults.sh.
# DOTHOME is derived from this script's location so it works when run standalone.

DOTHOME="$(cd "$(dirname "$0")/.." && pwd)"
exec "$DOTHOME/osx/set-defaults.sh"
